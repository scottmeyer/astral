schema_version = 1
schema_status = "experimental v1; bound worktrees, native save and same-account Git handoff implemented"
id = "astral"
name = "Astral"
description = "Native conversation continuity with capabilities rebound by the destination runtime"
core = "core"
projections = "projections"
work_items = "work/items.jsonl"

[identity]
scope = "repository-local logical names"
runtime_bindings = "private and external to Git"

[subsystems]
project-context = "core/project-context"
