schema_version = 1
schema_status = "implemented experimental v1; launch, bound workers, explicit save/recovery/completion and advisory hooks"
id = "astral"
name = "Astral"
description = "Git-backed project context, bound workers, native handoff and reviewed lifecycle operations"
core = "core"
projections = "projections"
work_items = "work/items.jsonl"

[identity]
scope = "repository-local logical names"
runtime_bindings = "private and external to Git"

[subsystems]
project-context = "core/project-context"
